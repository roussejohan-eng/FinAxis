// Réutilise exactement le même visuel que l'image Open Graph — pattern
// documenté par Next.js pour partager la génération entre les deux
// conventions de fichiers.
export { alt, size, contentType, default } from "./opengraph-image";
